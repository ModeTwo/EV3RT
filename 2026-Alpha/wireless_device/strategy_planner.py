"""Feature 10 strategy planning boundary."""

from typing import Any, Dict, List, Optional


class StrategyPlanner:
    # No.10の走行指示SEQアルゴリズムを実装するための境界だけを定義する。
    def build(self, password: Optional[str], hints: Dict[str, Any]) -> List[Dict[str, Any]]:
        # アルゴリズムは未実装のため空のSEQを返す。
        return []

